name: AI Training

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/ai-training.yml"
      - "package.json"
      - "package-lock.json"
      - "scripts/ai-lab.ts"
      - "scripts/train-bot.ts"
      - "src/core/**"
      - "!src/core/ai-lab/generated/**"
      - "!src/core/bots/generated/**"
  schedule:
    # Seven standard cycles per day, leaving a clear window for the deep pass.
    - cron: "17 0,6,9,12,15,18,21 * * *"
    # Daily deeper calibration pass, early morning in Europe/Paris.
    - cron: "47 2 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "Training intensity"
        required: true
        default: "standard"
        type: choice
        options:
          - quick
          - standard
          - deep

permissions:
  contents: write

concurrency:
  group: ai-training-main
  cancel-in-progress: ${{ github.event_name == 'push' }}

jobs:
  train:
    name: Train and publish AI snapshot
    runs-on: ubuntu-latest
    timeout-minutes: 330

    steps:
      - name: Checkout main
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Pick training profile
        id: profile
        shell: bash
        run: |
          EVENT_NAME="${{ github.event_name }}"
          MANUAL_MODE="${{ github.event.inputs.mode }}"
          SCHEDULE="${{ github.event.schedule }}"

          if [ "$EVENT_NAME" = "push" ]; then
            MODE="standard"
          elif [ "$EVENT_NAME" = "schedule" ] && [ "$SCHEDULE" = "47 2 * * *" ]; then
            MODE="deep"
          elif [ "$EVENT_NAME" = "schedule" ]; then
            MODE="standard"
          else
            MODE="${MANUAL_MODE:-standard}"
          fi

          echo "mode=$MODE" >> "$GITHUB_OUTPUT"

          case "$MODE" in
            quick)
              echo "iterations=1" >> "$GITHUB_OUTPUT"
              echo "population=4" >> "$GITHUB_OUTPUT"
              echo "elite_count=2" >> "$GITHUB_OUTPUT"
              echo "matches=4" >> "$GITHUB_OUTPUT"
              echo "promotion_matches=28" >> "$GITHUB_OUTPUT"
              echo "lab_matches=24" >> "$GITHUB_OUTPUT"
              echo "campaign_training_runs=1" >> "$GITHUB_OUTPUT"
              echo "campaign_promotion_runs=4" >> "$GITHUB_OUTPUT"
              echo "lab_adventure_runs=4" >> "$GITHUB_OUTPUT"
              ;;
            deep)
              echo "iterations=4" >> "$GITHUB_OUTPUT"
              echo "population=6" >> "$GITHUB_OUTPUT"
              echo "elite_count=2" >> "$GITHUB_OUTPUT"
              echo "matches=4" >> "$GITHUB_OUTPUT"
              echo "promotion_matches=56" >> "$GITHUB_OUTPUT"
              echo "lab_matches=48" >> "$GITHUB_OUTPUT"
              echo "campaign_training_runs=3" >> "$GITHUB_OUTPUT"
              echo "campaign_promotion_runs=12" >> "$GITHUB_OUTPUT"
              echo "lab_adventure_runs=16" >> "$GITHUB_OUTPUT"
              ;;
            standard)
              echo "iterations=3" >> "$GITHUB_OUTPUT"
              echo "population=6" >> "$GITHUB_OUTPUT"
              echo "elite_count=2" >> "$GITHUB_OUTPUT"
              echo "matches=4" >> "$GITHUB_OUTPUT"
              echo "promotion_matches=28" >> "$GITHUB_OUTPUT"
              echo "lab_matches=24" >> "$GITHUB_OUTPUT"
              echo "campaign_training_runs=2" >> "$GITHUB_OUTPUT"
              echo "campaign_promotion_runs=8" >> "$GITHUB_OUTPUT"
              echo "lab_adventure_runs=8" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "iterations=1" >> "$GITHUB_OUTPUT"
              echo "population=4" >> "$GITHUB_OUTPUT"
              echo "elite_count=2" >> "$GITHUB_OUTPUT"
              echo "matches=4" >> "$GITHUB_OUTPUT"
              echo "promotion_matches=28" >> "$GITHUB_OUTPUT"
              echo "lab_matches=24" >> "$GITHUB_OUTPUT"
              echo "campaign_training_runs=1" >> "$GITHUB_OUTPUT"
              echo "campaign_promotion_runs=4" >> "$GITHUB_OUTPUT"
              echo "lab_adventure_runs=4" >> "$GITHUB_OUTPUT"
              ;;
          esac

          echo "Selected AI training mode: $MODE"

      - name: Validate AI lab tests
        run: npm run test -- src/core/ai-lab/ai-lab.test.ts

      - name: Train candidate and promote if approved
        run: |
          npm run ai:train -- \
            --seed "${{ github.run_number }}" \
            --iterations "${{ steps.profile.outputs.iterations }}" \
            --population "${{ steps.profile.outputs.population }}" \
            --elite-count "${{ steps.profile.outputs.elite_count }}" \
            --matches-per-opponent "${{ steps.profile.outputs.matches }}" \
            --promotion-matches-per-opponent "${{ steps.profile.outputs.promotion_matches }}" \
            --campaign-training-runs "${{ steps.profile.outputs.campaign_training_runs }}" \
            --campaign-promotion-runs "${{ steps.profile.outputs.campaign_promotion_runs }}" \
            --search-depth 3 \
            --beam-width 12

      - name: Refresh AI lab snapshot
        run: |
          npm run ai:lab:apply -- \
            --matches "${{ steps.profile.outputs.lab_matches }}" \
            --adventure-runs "${{ steps.profile.outputs.lab_adventure_runs }}" \
            --seed "${{ github.run_number }}"

      - name: Typecheck generated files
        run: npx tsc --noEmit --pretty false

      - name: Commit training results
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add \
            src/core/bots/generated/trainedWeights.ts \
            src/core/bots/generated/liveChampion.ts \
            src/core/ai-lab/generated/latestAiLabReport.ts

          if git diff --cached --quiet; then
            echo "No AI changes to commit."
            exit 0
          fi

          git commit -m "Update AI training snapshot [skip ci]"
          git pull --rebase origin main
          git push origin HEAD:main
